import DiscoverMap from "@/components/discover/DiscoverMap";

// Same full-featured map as Discover: dark aubergine theme, live venue markers,
// pulsing busy indicators, horizontal bottom sheet, and search bar.
export default function MapPage() {
  return <DiscoverMap />;
}
